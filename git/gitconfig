[init]
  defaultBranch = main

[user]
  email = 50334817+AlanIsaacV@users.noreply.github.com
  user = AlanVazquez99
  name = Alan Vazquez
  signingkey = B30CB0CC45D09566

[alias]
  lg = log --graph \
    --abbrev-commit \
    --decorate \
    --all \
    --format=format:'\
%C(bold blue)%h%C(reset) \
%C(bold green)(%ar)%C(reset)\
%C(white)% s%C(reset) \
%C(yellow)%D%C(reset) \
%C(dim white)- %an%C(reset) \
%C(dim cyan)[%G?]%C(reset)\
'

[commit]
  gpgsign = true

[tag]
  gpgsign = true
